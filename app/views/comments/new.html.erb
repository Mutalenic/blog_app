<h2>Add new comment<h2>

    <%= form_with model: @comment, url: user_post_comments-url do |form|%>
        <%= form.text_area :text, class: 'form-control' %>
        <%= form.submit 'Add comment', class: 'btn btn-primary' %>
        <%= form.label:Comment%>
        <%= end %>

